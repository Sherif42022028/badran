# مراجعة تانية لموقع بن بدران — بعد التحديثات (2026-08-03)

الكوميت الأخير: `225b2a3 Fix: brand name (Budran), exact address, call/WhatsApp buttons, GA4 tracking`

| # | المبدأ | قبل | دلوقتي |
|---|--------|-----|--------|
| 1 | مطالبة ملف Google Business وربطه | ❌ | ❌ لسه زي ما هو |
| 2 | اتساق NAP | ⚠️ | ⚠️ اتحسن العنوان، لكن ظهر تعارض جديد في صيغة الرقم |
| 3 | الوصول السريع من الموبايل | ⚠️ | ✅ تقريبًا (زر الاتجاهات لينكه مكسور) |
| 4 | الاتساق مع الهوية | ✅ | ✅ |
| 5 | تحديث بدون مبرمج | ❌ | ❌ لسه زي ما هو |
| 6 | مؤشر النجاح | ❌ | ⚠️ الكود اتحط بس مش شغّال |

## اللي اتحل فعلاً ✅
- **زر "اتصل الآن (01020499680)" ظاهر في أول شاشة** على الموبايل (عند 418px من 844px) بدون سكرول، وتحته واتساب والمنيو.
- **شريط سفلي ثابت (sticky)** فيه "اتصل الآن" + "واتساب" ظاهر في كل الصفحة.
- **العنوان بقى دقيق**: `P786+PVF, Madinet Mit Ghamr (Include Daqados), Mit Ghamr, Dakahlia Governorate 7511463` — مطابق للي في خرائط جوجل.
- الاسم اللاتيني (Budran Coffee) اتوحّد في الهيرو والفوتر.
- أُضيف زر **"احصل على الاتجاهات"** في قسم الموقع.
- أُضيف كود GA4 وتتبع أحداث `call_click` و`whatsapp_click` في `src/lib/analytics.ts`.

## مشاكل لسه موجودة أو جديدة ❌

### 1) زر "احصل على الاتجاهات" لينكه مكسور
اللينك:
`https://www.google.com/maps/place/%D8%A8%D9%8BD8%A8%D8%AF%D8%B1%D8%A7%D9%86/@30.7168033,31.2621758,17z/data=...`
الترميز غلط (`%D9%8BD8%A8` — علامة `%` ناقصة)، فاسم المكان بيطلع "بًD8�دران". جربته: بيوصل لـ
`https://www.google.com/maps/place//@30.7168033,31.2621758,17z` — **دبوس إحداثيات فاضي بدون اسم المحل ولا بياناته**، مش ملف بن بدران.
الأفضل استخدام `https://www.google.com/maps/search/?api=1&query_place_id=...` أو ببساطة `https://maps.google.com/?cid=12148288070744934966`.
كمان الإحداثيات في الزر (31.2621758) مختلفة عن الخريطة المدمجة في نفس الصفحة (31.25960) — فرق ~250 متر.

### 2) GA4 مش شغّال — المعرّف لسه placeholder
في الـ HTML المنشور: `gtag/js?id=G-XXXXXXXXXX` و`gtag('config','G-XXXXXXXXXX')`.
السبب: `NEXT_PUBLIC_GA_ID` مش مضبوط في متغيرات البيئة على Vercel (`src/app/layout.tsx:74`)، فالكود fallback على القيمة الوهمية. **يعني صفر قياس فعلي** لمبدأ رقم 6 لحد ما تتحط قيمة حقيقية.

### 3) ملف خرائط جوجل لسه غير مُطالب بيه
فحصته تاني على `cid=12148288070744934966`: لسه ظاهر **"هل تملك هذا النشاط التجاري؟"**، ولسه **مفيش رقم تليفون ولا موقع إلكتروني** على الملف، والتصنيف لسه **"بارات"** (المفروض محمصة/متجر قهوة)، التقييم 4.6. ده بند مش بيتحل من الكود — محتاج صاحب المحل يعمل Claim.

### 4) تعارض جديد في صيغة الرقم (مبدأ 2)
- في الهيدر والهيرو وقسم التواصل: `01020499680`
- في **الفوتر**: `+20 102 049 9680`
- في كل روابط `tel:`: `+201020499680`
ده بالظبط النوع من الاختلاف اللي المبدأ التاني بيحذر منه. الأفضل توحيد شكل واحد معروض (مثلاً `01020499680`) مع `tel:+201020499680` في الـ href بس.

### 5) لسه ناقص من المراجعة الأولى
- **مفيش بيانات منظمة LocalBusiness (JSON-LD)** ولا `robots.txt` ولا `sitemap.xml` (الاتنين 404).
- **مفيش روابط فيسبوك/انستجرام** في الفوتر (الخطة كانت طالبة كده) — ومفيش `sameAs` يربط الموقع بالحسابات.
- **مفيش لوحة تحكم**: `/admin` لسه 404، والأسعار hardcoded في `src/data/menu.ts` → أي تغيير سعر محتاج مبرمج.
- الـ widget البرتقالي الخارجي (`rodlliai.vercel.app`) بيغطي جزء من الشريط السفلي على الموبايل.

## الترتيب المقترح للخطوة الجاية
1. تصليح لينك الاتجاهات (لينك cid).
2. حط `NEXT_PUBLIC_GA_ID` الحقيقي على Vercel (بدون كده التتبع مجرد ديكور).
3. توحيد صيغة الرقم المعروض.
4. JSON-LD + robots + sitemap + روابط السوشيال.
5. لوحة تحكم بسيطة للمنيو.
6. (خارج الكود) Claim ملف Google Business وتصحيح التصنيف وإضافة الرقم والمواعيد والموقع.
